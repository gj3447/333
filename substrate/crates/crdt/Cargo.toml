[package]
name = "p333-crdt"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "333 v2 substrate consistency lane (Lane B) — the CONVERGENCE-law LTDD receipt: replicas that exchange state must converge; a replica that didn't is surfaced as `replica_diverged`. Minimal state-based G-Counter exercises it; production adopts yrs (Yjs/Rust)."

[dependencies]
# The verification primitive: structured-event emission + store + 3-valued arrival verdict.
p333-ltdd = { path = "../ltdd" }
# Lane B production CRDT (Yjs/Rust). The convergence receipt is run against the REAL CRDT, not
# only the minimal G-Counter — the thorough completion of the lane.
yrs = "0.23"

[dev-dependencies]
serde_json = "1"

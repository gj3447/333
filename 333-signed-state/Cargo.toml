[package]
name = "signed_state333"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "333 P2P OS integration demo: signed LWW ops combining crdt333 + identity333"

# KG: SPAN_333_L5_L0_SignedCRDT_Integration, work-buffer-2026-04-18-scw-identity-shipped

[dependencies]
crdt333 = { path = "../333-crdt" }
identity333 = { path = "../333-identity" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
proptest = "1"

[package]
name = "incentive333"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0 OR MIT"
description = "333 P2P OS L14 Incentive — IncentiveCurve + SuperPeerSelector + RewardDistributor. Composes reputation333 (PeerScorer) + token333 (TokenLedger) + token333 (EmissionCurve)."

# KG: SPAN_333_L14_Incentive, plan-333-p2p-os-synthesis-execution-2026-04-18, queue-p11-super-peer-incentive-2026-04-18

[dependencies]
identity333 = { path = "../333-identity" }
reputation333 = { path = "../333-reputation" }
token333 = { path = "../333-token" }
thiserror = "1"

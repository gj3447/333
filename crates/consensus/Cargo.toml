[package]
name = "p333-consensus"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "333 v2 substrate Lane A — owned-object consistency (Sui-Lutris / FastPay style). Single-writer objects finalize via consistent broadcast WITHOUT global consensus; the safety law is NO DOUBLE-SPEND: each object version finalizes at most once, equivocating spends are rejected."

[dependencies]
p333-ltdd = { path = "../ltdd" }

[dev-dependencies]
serde_json = "1"

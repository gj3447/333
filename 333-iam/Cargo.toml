[package]
name = "iam333"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0 OR MIT"
description = "333 P2P OS L9 IAM — user/policy CredentialStore (SeaweedFS CredentialStore inspired)"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
thiserror = "1"
identity333 = { path = "../333-identity" }
crdt333 = { path = "../333-crdt" }

[package]
name = "transport333"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0 OR MIT"
description = "333 P2P OS L1 Transport — trait + in-memory mesh for deterministic integration testing"

# KG: SPAN_333_L1_Transport

[dependencies]
identity333 = { path = "../333-identity" }
thiserror = "1"

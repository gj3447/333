[package]
name = "mq333"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "333 P2P OS L7 Messaging — PubSub trait + in-memory broker (Kubo PubSubAPI inspired)"

[dependencies]
thiserror = "1"

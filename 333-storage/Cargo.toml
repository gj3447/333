[package]
name = "storage333"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "333 P2P OS L6 Storage — FilerStore trait + Decorator (SeaweedFS-inspired)"

[dependencies]
content333 = { path = "../333-content" }
thiserror = "1"

[package]
name = "compute-exchange333"
version = "0.1.0-alpha.1"
edition = "2021"
license = "AGPL-3.0-only"
description = "Consent-first deterministic kernel for the 333 browser compute exchange alpha"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
